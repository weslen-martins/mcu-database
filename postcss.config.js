module.exports = {
    plugins: {
        autoprefixer: {
            grid: true,
            browsers: [
                "last 4 versions",
                "Explorer >= 8",
            ]
        }
    },
}